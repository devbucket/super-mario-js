/**
 * CHR indices for the throwaway scroll demo (`src/demo/`).
 *
 * Pixel data always comes from `chr-tiles.ts`. This module only names indices
 * so the demo can share one backdrop tile; full 2×2 metatile tuples are spelled
 * out in `build-demo-metatile-table.ts` (derived from the cartridge metatile
 * graphics tables, not sequential placeholder order).
 */
export const skyBackdropTileIndex = 0x24;
